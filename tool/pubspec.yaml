name: nexus_tool
description: Repo tooling - currently the NEXUS app-icon generator (run from the repo root).
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  image: ^4.8.0
